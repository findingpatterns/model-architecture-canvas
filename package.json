{
  "name": "model-architecture-canvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Community catalog of model-architecture diagrams — preview in-browser, download the .canvas for your note editor.",
  "scripts": {
    "build": "node scripts/build-catalog.mjs && vite build && node scripts/assert-web-intact.mjs",
    "validate": "node scripts/build-catalog.mjs --check",
    "dev:editor": "vite",
    "test": "node --test editor-src/*.test.ts",
    "serve": "npm run build && (cd web && python3 -m http.server 8000)"
  },
  "dependencies": {
    "@xyflow/react": "^12.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
